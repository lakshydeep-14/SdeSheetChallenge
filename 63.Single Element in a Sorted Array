int uniqueElement(vector<int> arr, int n)
{
	// Write your code here
    
            int elem = 0;
            for (int i = 0; i < n; i++) {
                elem = elem ^ arr[i];
            }
            
            return elem;
}
